# rust-1337

Elite Rust development patterns. Decisions, not tutorials.

## What It Covers

| Domain | Topics |
|--------|--------|
| **CLI** | lexopt vs clap (ripgrep revelation), argument parsing |
| **Backend** | axum, sqlx, tonic, API patterns |
| **Frontend** | Leptos, Dioxus, WASM |
| **Native** | Tauri, egui, cross-platform |
| **Data-plane** | Pingora, Rama, proxy patterns |
| **Networking** | reqwest, Tower, protocols |
| **Async** | tokio gotchas, cancellation, blocking |
| **Embedded** | no_std, HALs, RTIC |
| **FFI/Unsafe** | CString traps, soundness |
| **Proc-macros** | syn, quote, derive patterns |
| **Ecosystem** | Crate selection, deprecations |
| **Tooling** | cargo, miri, criterion |

## Decision Framework

The skill picks winners:

- **HTTP server**: axum (not actix-web, not rocket)
- **Database**: sqlx (compile-time checked, not diesel)
- **gRPC**: tonic
- **CLI (simple)**: lexopt (what ripgrep uses)
- **CLI (complex)**: clap with derive
- **Frontend**: Leptos (fine-grained reactivity)
- **Native apps**: Tauri (not Electron)
- **Proxy/data-plane**: Pingora (Cloudflare) or Rama

## Reference Files

12 reference files loaded on-demand when Claude detects relevant context:

```
references/
├── cli.md          # Argument parsing patterns
├── backend.md      # Web services, APIs
├── frontend.md     # WASM, Leptos, Dioxus
├── native.md       # Desktop/mobile apps
├── data-plane.md   # Proxies, load balancers
├── networking.md   # HTTP clients, protocols
├── async.md        # Tokio patterns, gotchas
├── embedded.md     # no_std, HALs
├── ffi-unsafe.md   # FFI, unsafe patterns
├── proc-macros.md  # Derive, attribute macros
├── ecosystem.md    # Crate selection
└── tooling.md      # Cargo, testing, profiling
```

## Triggers

This skill activates when you're working on:
- Rust CLI, backend, frontend, or native apps
- Questions about crate selection
- Async/tokio patterns
- FFI or unsafe code
- Embedded Rust
- Proxy/data-plane development
